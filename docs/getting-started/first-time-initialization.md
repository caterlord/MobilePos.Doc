---
sidebar_position: 1
title: First-Time Initialization
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

:::info Who this is for
Store manager or setup owner preparing a terminal for first use.
:::

## Before you start

- Stable internet connection
- HQ login (`Email address or UserName` and `Password`)
- Correct brand/shop assignment in HQ
- Active subscription for the selected product type

## Steps

1. On `Select language`, choose `English`, `繁體中文`, or `简体中文`, then tap `Next`.
2. On `Login with HQ`, enter credentials and tap `Next`.
3. On `Select Brand and Shop`, choose brand and shop, then tap `Next`.
4. On `Select Product Type`, choose the terminal product type, then tap `Next`.
5. On `Select Mode`, choose terminal mode and tap `Next`.
6. On `Review your Settings`, verify details and tap `Activate`.
7. On `Downloading your settings`, wait until all setup items finish.

:::note Step list shown in the wizard
`Select language` -> `Login with HQ` -> `Select Brand and Shop` -> `Select Product Type` -> `Select Mode` -> `Review your Settings` -> `Downloading your settings`
:::

## What you should see

![Step 1 - Select language](/img/manual/en/getting-started/miss_0_select_language.png)
![Step 2 - Login with HQ](/img/manual/en/getting-started/initialize-step1-hq-login.png)
![Step 3 - Select Brand and Shop](/img/manual/en/getting-started/gs-02-initialize-step2-brand-shop.png)
![Step 4 - Select Product Type](/img/manual/en/getting-started/gs-03-initialize-step3-product-type.png)
![Step 6 - Review your Settings](/img/manual/en/getting-started/gs-04-initialize-step5-review-settings.png)
![Step 7 - Downloading your settings](/img/manual/en/getting-started/gs-05-initialize-step6-downloading.png)

:::note Screenshot pending
Step 5 (`Select Mode`) screenshot: `getting-started/gs-step5-select-mode.png`
:::

## After setup

<Tabs>
  <TabItem value="no-sync" label="No sync prompt">
1. Terminal returns to normal login flow.
2. Staff can sign in and start shift.
  </TabItem>
  <TabItem value="sync-required" label="Sync prompt appears">
1. Select sync days (`1`, `3`, `5`, or `7`).
2. Tap `Sync`.
3. After completion, tap the green completion button to continue.

![Order data sync prompt](/img/manual/en/getting-started/miss_2_sales_data_sync.png)
![Order data sync complete](/img/manual/en/getting-started/miss_1_sales_data_sync_complete.png)
  </TabItem>
</Tabs>

## If something goes wrong

:::warning `No Available Subscription`
Stop setup and verify subscription in HQ first.
:::

:::warning Cannot continue from `Login with HQ`
Check HQ credentials and internet connectivity, then retry.
:::

:::note Download step is stuck
Use `Retry` only when a setup item fails. Use `Reset` only if you need full restart.
:::

## When to ask manager/admin

- HQ account or shop assignment is incorrect
- Subscription is missing or expired
- Setup cannot complete after a clean retry
